# Example environment file for ExplicaSurf backend

# Coordinates for Stella Maris (Salvador-BA)
LAT=-12.9437
LON=-38.3539

# API keys (optional).  Leave empty if you haven't set them up yet.
STORMGLASS_API_KEY=
OPENWEATHER_API_KEY=
GEMINI_API_KEY=

# Tide API configuration (optional)
# If you want to fetch tide data from a third‑party service that exposes
# maré alta/baixa in JSON, set the URL here.  The backend will append
# ?location=${TIDE_LOCATION} when calling this service.
TIDE_API_URL=

# Name of the location used when calling the tide API.  For example,
# "Salvador" or "Porto de Salvador" depending on the service.
TIDE_LOCATION=Salvador